server:
  port: 8086

spring:
  application:
    name: payment-service
  mail:
    host: smtp.gmail.com
    port: 587
  
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: jdbc:mysql://localhost:3306/payment_db
    username: root
    password: "@,ASD123!!ria"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect


logging:
  level:
    root: INFO
    com.hotel.payment: DEBUG


